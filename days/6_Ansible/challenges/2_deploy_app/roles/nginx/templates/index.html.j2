<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>{{ site_title }}</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    body {
      background-color: #f5f5f5;
    }
    .hero {
      background: linear-gradient(135deg, #0d6efd, #6610f2);
      color: white;
      padding: 4rem 2rem;
      text-align: center;
    }
    .hero h1, .hero h2 {
      margin-bottom: 1rem;
    }
    .projects .card {
      transition: transform 0.2s ease-in-out;
    }
    .projects .card:hover {
      transform: scale(1.02);
    }
    .footer {
      background-color: #212529;
      color: #ccc;
      padding: 1rem;
      text-align: center;
      font-size: 0.9rem;
      margin-top: 4rem;
    }
  </style>
</head>
<body>

  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
      <a class="navbar-brand" href="#">{{ site_title }}</a>
    </div>
  </nav>

  <!-- Hero Section -->
  <header class="hero">
    <h1>{{ site_title }} </h1>
    <h2 class="text-light">{{ my_name }}</h2>
    <h4 class="fw-light">[ Deployed with Ansible and Vagrant ]</h4>
    <p class="text-light">Last deployed on {{ deployment_year }}</p>
  </header>

  <!-- Main Content -->
  <main class="container mt-5">
    <section class="mb-5">
      <h3>👋 About Me</h3>
      <p>
        Hello! I'm <strong>{{ my_name }}</strong>, a passionate developer who focus on DevOps.
        This is my personal portfolio. I enjoy building things and solving problems creatively.
      </p>
    </section>

    <section class="projects mb-5">
      <h3>🚀 Projects</h3>
      <div class="row row-cols-1 row-cols-md-2 g-4 mt-3">
        <div class="col">
          <div class="card h-100 shadow-sm">
            <div class="card-body">
              <h5 class="card-title">Project 1</h5>
              <p class="card-text">Brief description of my first project. Some tools, goals, or outcomes.</p>
              <a href="#" class="btn btn-primary">View Project</a>
            </div>
          </div>
        </div>

        <div class="col">
          <div class="card h-100 shadow-sm">
            <div class="card-body">
              <h5 class="card-title">Project 2</h5>
              <p class="card-text">Second project description goes here. Customize to reflect my experience.</p>
              <a href="#" class="btn btn-success">View Project</a>
            </div>
          </div>
        </div>

        <div class="col">
          <div class="card h-100 shadow-sm">
            <div class="card-body">
              <h5 class="card-title">Project 3</h5>
              <p class="card-text">Add a short summary of this project and highlight your role or tech used.</p>
              <a href="#" class="btn btn-warning">View Project</a>
            </div>
          </div>
        </div>
      </div>
    </section>
  </main>

  <!-- Footer -->
  <footer class="footer">
    &copy; {{ deployment_year }} - {{ my_name }} — Crafted with ❤️ and Ansible
  </footer>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
